<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button">
                <i class="fas fa-bars"></i>
            </a>
        </li>
    </ul>

    <ul class="navbar-nav ml-auto">
        @if (User.Identity.IsAuthenticated)
        {
        <li class="nav-item d-none d-sm-inline-block">
            <button type="submit" class="nav-link btn btn-link text-dark">@User.Identity.Name</button>
        </li>
        }
        else
        {
            <li class="nav-item d-none d-sm-inline-block">
                <button type="submit" class="nav-link btn btn-link text-dark">게스트</button>
            </li>
        }

        @if (User.Identity.IsAuthenticated)
        {
            <li class="nav-item d-none d-sm-inline-block">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Context.Request.Path">
                    <button type="submit" class="nav-link btn btn-link text-dark">로그아웃</button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item d-none d-sm-inline-block">
                <a asp-area="Identity" asp-page="/Account/Login" class="nav-link">로그인</a>
            </li>
            <li class="nav-item d-none d-sm-inline-block">
                <a asp-area="Identity" asp-page="/Account/Register" class="nav-link">계정 생성</a>
            </li>
        }
    </ul>
</nav>
<!-- /.navbar -->